package com.stocket.inventory.internal.domain;

public enum MovementType {
    RECEIVE,
    CONSUME,
    RETURN,
    ADJUSTMENT,
    LOSS,
    RETIRE,
    TRANSFER,
    TRANSFER_OUT,
    TRANSFER_IN,
    REVERSAL
}
