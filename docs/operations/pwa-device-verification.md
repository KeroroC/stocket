# PWA 实体设备验证记录

## 自动化验收

验收日期：2026-07-14

自动化环境：macOS、Playwright 1.61.1、Chromium 149、390x844 移动视口与 1440x900 桌面视口。

已完成：

- 已有物品从条码识别到入库提交在 30 秒预算内完成。
- fake scanner 分别注入商品条码和 `stocket:location:<code>` 位置码。
- 第三步草稿在断网、Service Worker 离线刷新后从 IndexedDB 恢复。
- 离线库存提交被阻止，恢复联网后可继续提交。
- 320、390、768、1440 px 宽度无横向滚动，移动固定导航为正文保留安全空间。
- 移动/桌面 Playwright 共 5 个场景通过。
- `PwaWorkflowAcceptanceTest` 验证条码搜索、位置码解析、入库、dashboard 查询和 VIEWER 写入拒绝。

自动化 scanner 使用事件驱动 fake，不申请真实摄像头权限，也不能替代实体设备的相机、安装与安全区检查。

## 实体手机检查

状态：**待人工执行**。当前 Codex 环境不能访问实体手机或真实摄像头，以下项目未伪造结果。

| 检查项 | 状态 | 记录要求 |
| --- | --- | --- |
| 浏览器与操作系统版本 | 未执行 | 记录系统、浏览器与版本，不记录账号或家庭数据 |
| HTTPS 安装到主屏幕 | 未执行 | 记录是否出现安装入口、standalone 启动结果 |
| 摄像头首次授权与拒绝后备 | 未执行 | 记录权限提示、拒绝后的手工输入路径 |
| 真实商品条码识别 | 未执行 | 使用测试条码；不得保存条码照片 |
| 真实位置码识别 | 未执行 | 使用测试位置码；不得记录真实家庭位置 |
| 页面隐藏后释放摄像头 | 未执行 | 切后台后确认摄像头指示灯关闭 |
| 断网刷新与草稿恢复 | 未执行 | 使用无敏感信息的测试草稿 |
| 刘海/圆角/底部安全区布局 | 未执行 | 检查固定导航、按钮和正文无重叠 |

人工执行后，只在本文件补充设备/浏览器、通过或失败、问题编号；不要提交 Cookie、真实家庭数据、条码照片或通知密钥。
